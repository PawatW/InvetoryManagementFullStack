sequenceDiagram
    actor Admin
    participant StaffPage as StaffAdminPage (app/(dashboard)/admin/staff/page.tsx)
    participant DataHook as useAuthedSWR('/staff')
    participant AddStaffForm as AddStaffForm.handleSubmit
    participant ApiFetch as apiFetch('/staff', ...)
    participant Backend as Inventory API

    Admin->>StaffPage: เปิดหน้า Staff Management
    StaffPage->>DataHook: useAuthedSWR('/staff', token)
    DataHook->>ApiFetch: apiFetch('/staff', { token })
    ApiFetch->>Backend: GET /staff
    Backend-->>StaffPage: รายชื่อพนักงาน

    Admin->>AddStaffForm: กรอกข้อมูลแล้ว submit
    AddStaffForm->>AddStaffForm: handleSubmit(event)
    AddStaffForm->>ApiFetch: apiFetch('/staff', { method: 'POST', body, token })
    ApiFetch->>Backend: POST /staff payload
    Backend-->>ApiFetch: สร้าง Staff สำเร็จ
    ApiFetch-->>AddStaffForm: Staff ที่สร้างแล้ว
    AddStaffForm->>AddStaffForm: form.reset()
    AddStaffForm->>StaffPage: onSuccess() → mutate()
    StaffPage->>DataHook: mutate()
    DataHook->>ApiFetch: apiFetch('/staff', { token })
    ApiFetch->>Backend: GET /staff (refresh)
    Backend-->>StaffPage: ข้อมูลอัปเดต
    StaffPage-->>Admin: แสดงข้อความสำเร็จ
